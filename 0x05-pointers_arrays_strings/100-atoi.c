#include "main.h"
#include <stdio.h>
#include <math.h>
/**
 * _atoi - converts
 * @s: the parameter
 *
 * Return: int
 */

int _atoi(char *s)
{

	int i, l = 0, num = 0;

	while (*(s + l))
	{
		l++;
	}

	for (i = 0; i < l; i++)
	{
		if (s[i] == 
	}

	return (num);
}
