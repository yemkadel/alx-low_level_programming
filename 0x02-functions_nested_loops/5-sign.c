#include "main.h"
/**
 * print_sign - this is a sign func
 *
 * Return: returns and integer
 */

int print_sign(int n)
{
	if (n > 0)
	{
		return (1);
	}
	else if (n  == 0)
	{
		return (0);
	}
	else
	{
		return (-1);
	}
}
