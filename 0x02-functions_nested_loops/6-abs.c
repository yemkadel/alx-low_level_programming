#include "main.h"
/**
 * _abs - the absolute func
 * @c: the  int to test
 *
 * Description: dec
 * Return: returns the abs value of c
 */

int _abs(int c)
{
	if (c < 0)
	{
		c = c * -1;
	}
	return (c);
}
